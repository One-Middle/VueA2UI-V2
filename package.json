{
  "name": "vue-a2ui-agent-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --parallel --filter @a2ui-platform/frontend --filter @a2ui-platform/backend dev",
    "dev:backend": "pnpm --filter @a2ui-platform/backend dev",
    "dev:frontend": "pnpm --filter @a2ui-platform/frontend dev",
    "setup": "pnpm install && pnpm --filter @a2ui-platform/shared build && cd packages/backend && npx prisma generate && npx prisma db push && cd ../.. && pnpm dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
