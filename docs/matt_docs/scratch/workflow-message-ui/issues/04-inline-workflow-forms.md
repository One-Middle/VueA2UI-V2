# 04 — Inline Workflow Forms

**What to build:** Clarification and decision forms appear inside the AI Workflow Message and continue the same Workflow instead of living in a separate Workflow panel.

**Blocked by:** 03 — Workflow Message Aggregation.

**Status:** ready-for-agent

- [ ] A waiting Clarification Form renders inside the relevant AI Workflow Message.
- [ ] A waiting Decision Form renders inside the relevant AI Workflow Message.
- [ ] Clarification submission uses the existing WorkflowAction API.
- [ ] Decision submission uses the existing WorkflowAction API.
- [ ] Required-field and revise-comment affordances remain visible and enforceable.
- [ ] The old Workflow panel form behavior is available through reusable inline form UI rather than duplicated ad hoc markup.
